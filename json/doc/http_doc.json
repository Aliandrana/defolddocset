{
  "elements": [
    {
      "description": "Perform a HTTP request.", 
      "parameters": [
        {
          "doc": "target url", 
          "name": "url"
        }, 
        {
          "doc": "HTTP method, e.g. GET/PUT/POST/DELETE/...", 
          "name": "method"
        }, 
        {
          "doc": "response callback", 
          "name": "callback"
        }, 
        {
          "doc": "optional lua-table with custom headers", 
          "name": "[headers]"
        }, 
        {
          "doc": "optional data to send", 
          "name": "[post_data]"
        }, 
        {
          "doc": "optional lua-table with request parameters. Supported entries: 'timeout'=<number> (in seconds)", 
          "name": "[options]"
        }
      ], 
      "brief": "perform a HTTP request", 
      "examples": "<p>\nBasic HTTP-GET request. The callback receives a table with the response\nin the fields status, the response (the data) and headers (a table).\n</p>\n<pre>\nlocal function http_result(self, id, response)\n    print(response.status)\n    print(response.response)\n    pprint(response.headers)\nend\nfunction example(self)\n    http.request(\"http://www.google.com\", \"GET\", http_result)\nend\n</pre>", 
      "type": "FUNCTION", 
      "return_": "", 
      "name": "http.request"
    }
  ]
}